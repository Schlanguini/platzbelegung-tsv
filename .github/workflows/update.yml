name: Update calendars

on:
  schedule:
    # 04:00 Berlin
    - cron: "0 2 * * *"   # Sommerzeit (UTC+2)
    - cron: "0 3 * * *"   # Winterzeit (UTC+1)

    # 12:00 Berlin
    - cron: "0 10 * * *"  # Sommerzeit
    - cron: "0 11 * * *"  # Winterzeit

    # 15:00 Berlin
    - cron: "0 13 * * *"  # Sommerzeit
    - cron: "0 14 * * *"  # Winterzeit

    # 19:00 Berlin
    - cron: "0 17 * * *"  # Sommerzeit
    - cron: "0 18 * * *"  # Winterzeit

    # 21:00 Berlin
    - cron: "0 19 * * *"  # Sommerzeit
    - cron: "0 20 * * *"  # Winterzeit

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Generate calendars
        run: python scripts/update_calendars.py

      - name: Commit changes
        run: |
          git config user.name github-actions
          git config user.email github-actions@github.com
          git add output/*.ics
          git commit -m "update calendars" || exit 0
          git push
